apiVersion: mesh.webmesh.io/v1
kind: NodeGroup
metadata:
  name: nodegroup-sample
spec:
  replicas: 1
  cluster:
    imagePullPolicy: Always
  config:
    logLevel: debug
  mesh:
    name: mesh-sample
